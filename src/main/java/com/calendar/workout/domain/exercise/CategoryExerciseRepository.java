package com.calendar.workout.domain.exercise;

import org.springframework.data.jpa.repository.JpaRepository;

public interface CategoryExerciseRepository extends JpaRepository<CategoryExercise, Long> {
}
